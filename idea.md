a linux ricing utility that can
- identify distro
-identify wm
-identify package managers
- identify hardware
.After that ask the user to input a github link that corresponds to a linux rice/dotfile
.then it should call a free model(preferrably without api key), which should install the rice all by itself
.after install it should change the default to the new rice config instead of the old config
.the residual files must be removed after install
.also the tool should be publishable(use version control)
.also try to make it a cURL command
